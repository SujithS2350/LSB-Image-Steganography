# LSB-Image-Steganography
This project implements LSB (Least Significant Bit) steganography — a technique for hiding secret data inside a BMP image file without visibly altering it. Each byte of pixel data has its lowest bit replaced with one bit of the secret message. Since the LSB contributes almost nothing to a pixel's color value,.
